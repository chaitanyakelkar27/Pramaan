{
  "name": "pramaan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preflight:sepolia": "node scripts/preflight-sepolia.js",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify:sepolia": "hardhat run scripts/verify.js --network sepolia",
    "sync:frontend:sepolia": "node scripts/sync-frontend-env.js sepolia",
    "deploy:sepolia:sync": "npm run preflight:sepolia && npm run deploy:sepolia && npm run sync:frontend:sepolia",
    "deploy:sepolia:full": "npm run deploy:sepolia:sync && npm run verify:sepolia"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^17.3.1",
    "ethers": "^6.16.0"
  }
}
